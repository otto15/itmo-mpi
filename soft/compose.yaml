services:
  postgres:
    image: postgres:16-alpine
    environment:
      POSTGRES_DB: drakkar
      POSTGRES_USER: ${DB_USER:-drakkar}
      POSTGRES_PASSWORD: ${DB_PASSWORD:-drakkar}
    volumes:
      - drakkar-data:/var/lib/postgresql/data
    healthcheck:
      test: ["CMD-SHELL", "pg_isready -U $${POSTGRES_USER} -d $${POSTGRES_DB}"]
      interval: 5s
      timeout: 3s
      retries: 10
    restart: unless-stopped

  backend:
    build:
      context: .
      dockerfile: backend/Dockerfile
    environment:
      DB_URL: jdbc:postgresql://postgres:5432/drakkar
      DB_USER: ${DB_USER:-drakkar}
      DB_PASSWORD: ${DB_PASSWORD:-drakkar}
      PROVISIONING_KEY: ${PROVISIONING_KEY:-local-provisioning-key}
    depends_on:
      postgres:
        condition: service_healthy
    ports:
      - "${APP_PORT:-8088}:8080"
    healthcheck:
      test: ["CMD", "curl", "--fail", "--silent", "http://localhost:8080/actuator/health"]
      interval: 10s
      timeout: 3s
      retries: 10
    restart: unless-stopped

volumes:
  drakkar-data:
